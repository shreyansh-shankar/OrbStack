# PagerDuty Alert

At 03:14 UTC, our web service alert fired: **HTTP 502 Bad Gateway**.
The primary NGINX proxy is up, but the backend application service is refusing connections. You have been paged as the on-call engineer to investigate.
